# src

This folder contains lightweight, reusable helpers used across scripts.

## Contents

- `utils/` — environment variable helpers, dataset I/O, stats I/O.
- `meta/` — minimal schema checks for `info.json` and `stats.json`.

These utilities keep scripts consistent and avoid duplicating small helpers.
