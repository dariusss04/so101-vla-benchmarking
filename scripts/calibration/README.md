# Calibration

Robot calibration helpers for follower and leader devices.

- `calibrate_follower.py` / `.sh`
- `calibrate_leader.py` / `.sh`

All ports, IDs, and calibration paths are configured via environment variables.
