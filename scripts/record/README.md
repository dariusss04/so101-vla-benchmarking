# Recording

Scripts for recording teleoperated demonstrations into LeRobot datasets.

- `record_dataset.py` — Python entry point using env vars for repo ID, root, task description, ports, and camera settings.
- `record_dataset.sh` — shell wrapper with multiline flags.

Use these to create new datasets when you change lighting, camera placement, or task setup.
