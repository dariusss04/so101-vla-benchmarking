# Scripts

This folder contains entry‑point scripts for teleoperation, data recording, calibration, motor configuration, training, and inference. All scripts are **environment‑variable driven** so the repo stays portable (no hard‑coded ports, paths, or repo IDs).

Each subfolder has its own README with example usage and expected environment variables.
