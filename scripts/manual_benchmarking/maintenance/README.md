# Maintenance

Small maintenance utilities for dataset cleanup and stats generation.

- `delete_episodes.py` — remove specific episode indices from a dataset.
- `compute_stats.py` — recompute `meta/stats.json` for a dataset.

All paths and repo IDs are configured via environment variables.
