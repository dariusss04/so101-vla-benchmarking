# Motor Configuration

Scripts for applying motor configuration and safety limits on the SO101 follower/leader.

- `configure_follower_motors.py` / `.sh`
- `configure_leader_motors.py` / `.sh`

All device ports and IDs are provided through environment variables.
