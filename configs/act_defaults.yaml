# Default ACT training hyperparameters.

training:
  batch_size: 16
  steps: 100000
  log_freq: 50
  save_checkpoint: true
  save_freq: 10000
  use_vae: true
  use_amp: true
  seed: 42
  num_workers: 4
  video_backend: pyav

optimizer:
  preset: default

scheduler:
  preset: default
